# EditorConfig is awesome:http://EditorConfig.org

# top-most EditorConfig file
root = true

# Baseline
[*]
charset = utf-8
indent_style = tab
trim_trailing_whitespace = true
max_line_length = 144

# Unreal Engine prefers CRLF
end_of_line = crlf